Optional: place a "ring.blp" or "ring.tga" file here for a custom ring texture.
The addon works without it — it falls back to a built-in WoW glow texture.
A simple white circle/ring on a transparent background (256x256 or 512x512) works well.
